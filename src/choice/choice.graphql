type Choice {
  id: ID!
  text: String!
  score: Int!
  question: Question!
}

type Query {
  getAllChoice: [Question]
  getAllChoiceByQuestionId(questionId: ID): [Question]
}

type Mutation {
  createChoice(text: String!, score: Int!, questionId: ID!): Choice
}
