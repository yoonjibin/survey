type Question {
  id: ID!
  question: String!
  choices: [Choice]
}

type Query {
  findAllBySurveyId(surveyId: ID!): Question
}

type Mutation {
  createQuestion(question: String!, surveyId: ID!): Question
}
