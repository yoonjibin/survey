type Question {
  id: ID!
  question: String!
  choices: [Choice]
}

type Query {
  getAllQuestionBySurveyId(surveyId: ID): [Question]
}

type Mutation {
  createQuestion(question: String!, surveyId: ID!): Question
}
