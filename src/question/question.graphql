type Question {
  id: ID!
  question: String!
  choices: [Choice]
}

type Query {
  findAllQuestionBySurveyId(surveyId: ID!): Question
}

type Mutation {
  createQuestion(question: String!, surveyId: ID!): Question
}
