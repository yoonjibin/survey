type Survey {
  id: ID!
  title: String!
  isCompleted: Boolean
  questions: [Question]
}

type Query {
  getAllSurvey: [Survey]
  getById(id: ID): Survey
}

type Mutation {
  createSurvey(title: String!): Survey
  updateSurvey(id: ID!, title!): Survey
}
