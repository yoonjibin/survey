type Survey {
  id: ID!
  title: String!
  isCompleted: Boolean
  questions: [Question]!
}

type Query {
  getAllSurvey(): [Survey]
  getSurvey(id: ID!): Survey
}

type Mutation {
  createSurvey(title: String!): Survey
}
